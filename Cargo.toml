[package]
name = "rs-util"
version = "0.1.0"
edition = "2024"

[features]
io = []
arc_cow = []
async = ["dep:async-task"]
cell = []
id = ["dep:nohash-hasher", "dep:ahash"]
log = []
macros = []
option = []
shared_string = []
toggle = []
range = []
builder = []
retainable = []
copy_on_write = []
deferred = []

all = [
    "io",
    "arc_cow",
    "async",
    "cell",
    "id",
    "log",
    "macros",
    "option",
    "shared_string",
    "toggle",
    "range",
    "builder",
    "retainable",
    "copy_on_write",
    "deferred",
]


[dependencies]
tracing = "0.1"
serde = { version = "1.0", features = ["derive"] }
derive_more = { version = "2.0.0", features = ["full"] }

parking_lot = "0.12"
async-task = { version = "4.7.1", optional = true }
nohash-hasher = { version = "0.2.0", optional = true }
ahash = { version = "0.8.12", optional = true }
rustc-hash = "2.1.1"


anyhow = "1.0.86"
rand = "0.9.1"
